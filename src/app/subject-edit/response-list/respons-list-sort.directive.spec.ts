import { ResponsListSortDirective } from './respons-list-sort.directive';

describe('ResponsListSortDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsListSortDirective();
    expect(directive).toBeTruthy();
  });
});
